<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yemreak.retrosnakerpg">

    <application
        android:icon="@mipmap/snake_game"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/snake_game"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SnakeActivity1" />
        <activity android:name=".SnakeActivity2" />
        <activity android:name=".OptionsActivity" />
        <activity android:name=".GameModeActivity"/>

    </application>

</manifest>